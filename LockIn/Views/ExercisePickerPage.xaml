<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LockIn.ViewModels"
             xmlns:views="clr-namespace:LockIn.Views"
             xmlns:controls="clr-namespace:LockIn.Controls"
             xmlns:loc="clr-namespace:LockIn.Resources.Strings"
             x:Class="LockIn.Views.ExercisePickerPage"
             x:DataType="vm:ExercisePickerViewModel"
             Title="{loc:Localize ExercisePicker_PageTitle}"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False"
             BackgroundColor="Transparent">

    <ContentPage.Resources>
        <views:EquipmentLabelConverter x:Key="EquipmentLabelConverter"/>
        <views:EquipmentNotOtherConverter x:Key="EquipmentNotOtherConverter"/>
        <views:StringNotEmptyConverter x:Key="StringNotEmptyConverter"/>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" SafeAreaEdges="None">

        <!-- Atmosf�risk bakgrund -->
        <controls:AtmosphericBackgroundView Grid.RowSpan="5" InputTransparent="True"/>

        <Grid Grid.Row="0" Padding="16,56,16,8" ColumnDefinitions="48,*">
            <Border Grid.Column="0"
                    WidthRequest="36" HeightRequest="36"
                    StrokeShape="Ellipse"
                    BackgroundColor="#17FFFFFF"
                    Stroke="#29FFFFFF"
                    StrokeThickness="0.5"
                    VerticalOptions="Center">
                <Path Style="{StaticResource BackChevron}"/>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnBackClicked"/>
                </Border.GestureRecognizers>
            </Border>
            <Label Grid.Column="1" Text="{loc:Localize ExercisePicker_Title}"
                   Style="{StaticResource DisplayLabel}"
                   FontSize="22" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Grid>

        <CollectionView Grid.Row="1" Grid.RowSpan="4"
                        ItemsSource="{Binding FilteredExercises}"
                        SelectionMode="None"
                        HorizontalScrollBarVisibility="Never"
                        VerticalScrollBarVisibility="Never">
            <CollectionView.Header>
                <VerticalStackLayout Spacing="0">
                    <!-- Pill-formad s�kruta (scrollar med) -->
                    <Border BackgroundColor="{StaticResource ForgeSurface2}"
                            StrokeShape="RoundRectangle 26"
                            StrokeThickness="0"
                            Padding="18,0"
                            HeightRequest="46"
                            Margin="16,0,16,10">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                            <Label Grid.Column="0"
                                   Text="🔍"
                                   FontSize="20"
                                   TextColor="{StaticResource ForgeMuted}"
                                   VerticalOptions="Center"/>
                            <Entry Grid.Column="1"
                                   Text="{Binding Source={RelativeSource AncestorType={x:Type vm:ExercisePickerViewModel}}, Path=SearchText}"
                                   Placeholder="{loc:Localize ExercisePicker_SearchPlaceholder}"
                                   PlaceholderColor="{StaticResource ForgeMuted}"
                                   BackgroundColor="Transparent"
                                   TextColor="{StaticResource ForgeText}"
                                   FontFamily="DMSansRegular" FontSize="15"
                                   VerticalOptions="Center"/>
                        </Grid>
                    </Border>
                    <!-- Muskelchips (horisontell scroll) -->
                    <ScrollView Orientation="Horizontal"
                                HorizontalScrollBarVisibility="Never"
                                Margin="16,0,16,8">
                        <HorizontalStackLayout BindableLayout.ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type vm:ExercisePickerViewModel}}, Path=MuscleChips}"
                                               Spacing="6">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="vm:MuscleGroupChip">
                                    <Border BackgroundColor="{Binding Background}"
                                            StrokeShape="RoundRectangle 22" StrokeThickness="0"
                                            Padding="14,0"
                                            HeightRequest="34">
                                        <Label Text="{Binding Label}"
                                               TextColor="{Binding Foreground}"
                                               FontFamily="BebasNeue" FontSize="13" CharacterSpacing="1"
                                               VerticalOptions="Center"/>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ExercisePickerViewModel}}, Path=SelectChipCommand}"
                                                CommandParameter="{Binding .}"/>
                                        </Border.GestureRecognizers>
                                    </Border>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </HorizontalStackLayout>
                    </ScrollView>
                    <!-- Equipment-chips (horisontell scroll) -->
                    <ScrollView Orientation="Horizontal"
                                HorizontalScrollBarVisibility="Never"
                                Margin="16,0,16,8">
                        <HorizontalStackLayout
                            BindableLayout.ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type vm:ExercisePickerViewModel}}, Path=EquipmentChips}"
                            Spacing="6">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="vm:EquipmentChip">
                                    <Border BackgroundColor="{Binding Background}"
                                            StrokeShape="RoundRectangle 22" StrokeThickness="0"
                                            Padding="14,0" HeightRequest="34">
                                        <Label Text="{Binding Label}"
                                               TextColor="{Binding Foreground}"
                                               FontFamily="BebasNeue" FontSize="13" CharacterSpacing="1"
                                               VerticalOptions="Center"/>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ExercisePickerViewModel}}, Path=SelectEquipmentChipCommand}"
                                                CommandParameter="{Binding .}"/>
                                        </Border.GestureRecognizers>
                                    </Border>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </HorizontalStackLayout>
                    </ScrollView>
                    <!-- Add custom exercise row -->
                    <Border Margin="16,0,16,8"
                            StrokeShape="RoundRectangle 18"
                            BackgroundColor="{AppThemeBinding Light={StaticResource LightSurface}, Dark={StaticResource ForgeSurface}}"
                            StrokeThickness="0"
                            Padding="18,14">
                        <Border.Shadow>
                            <Shadow Brush="Black" Offset="0,2" Radius="10" Opacity="0.18"/>
                        </Border.Shadow>
                        <Grid ColumnDefinitions="24,*" ColumnSpacing="10">
                            <Label Grid.Column="0"
                                   Text="+"
                                   FontSize="22" FontAttributes="Bold"
                                   TextColor="{StaticResource ForgeAccentOrange}"
                                   VerticalOptions="Center" HorizontalOptions="Center"/>
                            <Label Grid.Column="1"
                                   Text="{loc:Localize ExercisePicker_CreateCustom}"
                                   FontFamily="DMSansMedium" FontSize="15"
                                   TextColor="{AppThemeBinding Light={StaticResource LightText}, Dark={StaticResource ForgeText}}"
                                   VerticalOptions="Center"/>
                        </Grid>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnAddCustomExerciseTapped"/>
                        </Border.GestureRecognizers>
                    </Border>
                </VerticalStackLayout>
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:ExercisePickerRow">
                    <Border Margin="16,0,16,8"
                            BackgroundColor="{AppThemeBinding Light={StaticResource LightSurface}, Dark={StaticResource ForgeSurface}}"
                            StrokeShape="RoundRectangle 18"
                            StrokeThickness="0"
                            Padding="18,14">
                        <Border.Shadow>
                            <Shadow Brush="Black" Offset="0,2" Radius="10" Opacity="0.18"/>
                        </Border.Shadow>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ExercisePickerViewModel}}, Path=SelectExerciseCommand}"
                                CommandParameter="{Binding .}"/>
                            <PointerGestureRecognizer
                                PointerPressed="OnExercisePointerPressed"
                                PointerReleased="OnExercisePointerReleased"/>
                        </Border.GestureRecognizers>
                        <Grid ColumnDefinitions="12,*,Auto" ColumnSpacing="12">
                            <Ellipse Grid.Column="0"
                                     WidthRequest="10" HeightRequest="10"
                                     Fill="{Binding MuscleColor}"
                                     VerticalOptions="Center"/>
                            <VerticalStackLayout Grid.Column="1" Spacing="3" VerticalOptions="Center">
                                <Label Text="{Binding Name}"
                                       FontFamily="DMSansMedium" FontSize="15"/>
                                <Label Text="{Binding MuscleLabel}"
                                       FontSize="12"
                                       Opacity="0.85"
                                       TextColor="{Binding MuscleColor}"/>
                                <Label Text="{Binding EquipmentLabel}"
                                       IsVisible="{Binding EquipmentLabel, Converter={StaticResource StringNotEmptyConverter}}"
                                       FontFamily="BebasNeue" FontSize="10" CharacterSpacing="1"
                                       TextColor="{AppThemeBinding Light={StaticResource LightMuted}, Dark={StaticResource ForgeMuted}}"
                                       VerticalOptions="Center"/>
                            </VerticalStackLayout>
                            <Border Grid.Column="2"
                                    IsVisible="{Binding IsCustom}"
                                    BackgroundColor="{StaticResource ForgeAccentOrangeDim}"
                                    StrokeShape="RoundRectangle 8"
                                    StrokeThickness="0" Padding="8,4"
                                    VerticalOptions="Center">
                                <Label Text="{loc:Localize Library_Badge_Custom}" TextColor="{StaticResource ForgeAccentOrange}"
                                       FontSize="10" FontFamily="BebasNeue" CharacterSpacing="1"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
